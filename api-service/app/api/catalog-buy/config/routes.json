{
  "routes": [
    {
      "method": "GET",
      "path": "/catalog-buy",
      "handler": "catalog-buy.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/catalog-buy/count",
      "handler": "catalog-buy.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/catalog-buy/:id",
      "handler": "catalog-buy.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/catalog-buy",
      "handler": "catalog-buy.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/catalog-buy/:id",
      "handler": "catalog-buy.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/catalog-buy/:id",
      "handler": "catalog-buy.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
